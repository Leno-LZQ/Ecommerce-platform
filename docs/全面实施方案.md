# 基于 Spring Boot 全栈技术的高并发电商系统 —— 全面实施方案

> **版本：** v1.0  
> **适用周期：** 6 周（可压缩至 4 周核心链路）  
> **前置条件：** JDK 17+、Node 18+、Docker Desktop、16GB RAM（推荐）

---

## 目录

- [阶段零：环境准备（Day 0~1）](#阶段零环境准备day-01)
- [阶段一：项目脚手架与基础设施（第 1 周）](#阶段一项目脚手架与基础设施第-1-周)
- [阶段二：用户模块 + 安全体系（第 2 周）](#阶段二用户模块--安全体系第-2-周)
- [阶段三：商品模块 + 缓存体系（第 3 周）](#阶段三商品模块--缓存体系第-3-周)
- [阶段四：购物车 + 订单核心链路（第 4 周）](#阶段四购物车--订单核心链路第-4-周)
- [阶段五：支付 + 搜索 + 管理后台（第 5 周）](#阶段五支付--搜索--管理后台第-5-周)
- [阶段六：集成测试 + 压测 + 部署（第 6 周）](#阶段六集成测试--压测--部署第-6-周)
- [阶段七：答辩准备](#阶段七答辩准备)
- [附录](#附录)

---

## 阶段零：环境准备（Day 0~1）

### 0.1 安装清单

| 软件 | 版本要求 | 用途 | 安装方式 |
|------|---------|------|---------|
| **IntelliJ IDEA** | 2024.x+ Ultimate | 主 IDE | 官网下载，学生可申请免费 Ultimate 版 |
| **Docker Desktop** | 最新版 | 中间件容器化 | [docker.com](https://www.docker.com/products/docker-desktop/) 下载 |
| **Git** | 2.x+ | 版本控制 | ✅ 已安装 `v2.54.0` |
| **JDK** | 17+ | 编译运行 | ✅ 已安装 `v21.0.5` |
| **Node.js + npm** | 18+ | 前端构建 | ✅ 已安装 `v22.22.2` / `npm 10.9.7` |
| **Postman / ApiFox** | 最新版 | API 调试 | 选装，也可用 Knife4j |
| **JMeter** | 5.x+ | 压力测试 | [jmeter.apache.org](https://jmeter.apache.org/) 下载 |

### 0.2 Docker Desktop 安装（关键）

```bash
# 1. 前往 https://www.docker.com/products/docker-desktop/ 下载 Docker Desktop for Windows
# 2. 安装后启动，确保右下角鲸鱼图标亮起
# 3. 验证安装
docker --version
docker compose version
```

> ⚠️ **Windows 注意**：如果未安装 WSL2，Docker Desktop 会引导你安装。安装后务必在 PowerShell 中执行 `wsl --set-default-version 2` 启用 WSL2 模式（比 Hyper-V 模式性能更好）。

### 0.3 确认机器内存

> ⚠️ **建议**：打开任务管理器 → 性能 → 内存，确认你的物理内存大小。

| 内存 | 可行方案 |
|:----:|---------|
| **16GB+** | 全量方案（含 ES + Prometheus） |
| **12GB** | 标配方案（去掉 Prometheus + Grafana） |
| **8GB** | 轻量版（去掉 ES + Prometheus + Grafana） |
| **< 8GB** | 极简版（去掉 ES + RabbitMQ + Nacos + Sentinel） |

### 0.4 IDEA 插件推荐

```
- Lombok              → @Data/@Slf4j 自动生成
- MyBatisX            → MyBatis-Plus 代码生成 + XML 跳转
- Gradle               → 已内置
- Docker              → 已内置
- Spring Assistant    → Spring Boot 配置提示
- Rainbow Brackets    → 括号着色，调试时省眼
- GitToolBox          → Git 状态显示
```

---

## 阶段一：项目脚手架与基础设施（第 1 周）

### 1.1 项目结构全貌

```
ecommerce-platform/
├── build.gradle.kts              # 根构建文件
├── settings.gradle.kts           # 多模块声明
├── gradle.properties             # 版本统一管理
├── gradlew / gradlew.bat         # Gradle Wrapper
├── gradle/
│   └── wrapper/
├── docker/                       # Docker Compose 编排
│   ├── docker-compose.yml        # 中间件容器
│   ├── docker-compose.app.yml    # 应用容器
│   └── mysql/
│       └── init.sql              # 初始化建表脚本
├── common/                       # 公共模块
│   ├── src/main/java/.../common/
│   │   ├── result/               # 统一返回体
│   │   ├── exception/            # 全局异常处理
│   │   ├── constant/             # 常量定义
│   │   └── util/                 # 工具类
│   └── build.gradle.kts
├── user-service/                 # 用户服务 (端口 8081)
├── product-service/              # 商品服务 (端口 8082)
├── cart-service/                 # 购物车服务 (端口 8083)
├── order-service/                # 订单服务 (端口 8084)
├── payment-service/              # 支付服务 (端口 8085)
├── inventory-service/            # 库存服务 (端口 8086)
├── gateway/                      # Spring Cloud Gateway (端口 8080)
├── admin-service/                # 运维控制台（包含 Dashboard）
└── frontend/                     # Vue 3 管理后台
    ├── package.json
    ├── vite.config.ts
    └── src/
```

### 1.2 Gradle 多模块搭建

**settings.gradle.kts**

```kotlin
rootProject.name = "ecommerce-platform"

include(
    "common",
    "user-service",
    "product-service",
    "cart-service",
    "order-service",
    "payment-service",
    "inventory-service",
    "gateway",
    "admin-service"
)
```

**根 build.gradle.kts**

```kotlin
plugins {
    id("java")
    id("org.springframework.boot") version "3.2.5" apply false
    id("io.spring.dependency-management") version "1.1.4" apply false
}

allprojects {
    group = "com.ecommerce"
    version = "1.0.0"

    repositories {
        mavenCentral()
        maven { url = uri("https://maven.aliyun.com/repository/public") }
    }
}

subprojects {
    apply(plugin = "java")

    java {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    tasks.withType<Test> {
        useJUnitPlatform()
    }
}
```

**各子模块通用 build.gradle.kts** 模板：

```kotlin
plugins {
    id("org.springframework.boot")
    id("io.spring.dependency-management")
}

dependencies {
    implementation(project(":common"))

    // Spring Boot
    implementation("org.springframework.boot:spring-boot-starter-web")
    implementation("org.springframework.boot:spring-boot-starter-validation")

    // MyBatis-Plus
    implementation("com.baomidou:mybatis-plus-spring-boot3-starter:3.5.7")

    // MySQL
    runtimeOnly("com.mysql:mysql-connector-j")

    // Redis
    implementation("org.springframework.boot:spring-boot-starter-data-redis")

    // Nacos
    implementation("com.alibaba.cloud:spring-cloud-starter-alibaba-nacos-discovery:2023.0.1.2")
    implementation("com.alibaba.cloud:spring-cloud-starter-alibaba-nacos-config:2023.0.1.2")

    // Lombok
    compileOnly("org.projectlombok:lombok")
    annotationProcessor("org.projectlombok:lombok")

    // Test
    testImplementation("org.springframework.boot:spring-boot-starter-test")
}
```

### 1.3 Gradle Wrapper 初始化

```bash
# 在项目根目录执行
cd D:\study\java全栈开发\Java全栈技术项目
gradle wrapper --gradle-version 8.7
```

> 如果没有全局 Gradle，可手动下载 `gradle-wrapper.jar` 并创建 `gradle-wrapper.properties`，或者从其他项目复制 `gradlew` 脚本。

### 1.4 中间件 Docker Compose 编排

创建 `docker/docker-compose.yml`：

```yaml
version: "3.8"

services:
  # ========== MySQL ==========
  mysql:
    image: mysql:8.0
    container_name: ecommerce-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: ecommerce
    ports:
      - "3306:3306"
    volumes:
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
      - mysql-data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password
            --character-set-server=utf8mb4
            --collation-server=utf8mb4_unicode_ci

  # ========== Redis ==========
  redis:
    image: redis:7.2
    container_name: ecommerce-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  # ========== RabbitMQ ==========
  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: ecommerce-rabbitmq
    ports:
      - "5672:5672"   # AMQP
      - "15672:15672" # 管理后台
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: admin123

  # ========== Nacos ==========
  nacos:
    image: nacos/nacos-server:v2.3.2
    container_name: ecommerce-nacos
    ports:
      - "8848:8848"
      - "9848:9848"
    environment:
      MODE: standalone
      MYSQL_SERVICE_HOST: mysql
      MYSQL_SERVICE_DB_NAME: nacos_config
      MYSQL_SERVICE_USER: root
      MYSQL_SERVICE_PASSWORD: root123
    depends_on:
      - mysql

  # ========== Sentinel Dashboard ==========
  sentinel:
    image: bladex/sentinel-dashboard:1.8.8
    container_name: ecommerce-sentinel
    ports:
      - "8858:8858"
    environment:
      JAVA_OPTS: "-Dserver.port=8858"
    depends_on:
      - nacos

  # ========== Elasticsearch (可选：轻量版跳过) ==========
  elasticsearch:
    image: elasticsearch:8.13.0
    container_name: ecommerce-es
    environment:
      - discovery.type=single-node
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
      - xpack.security.enabled=false
    ports:
      - "9200:9200"
    volumes:
      - es-data:/usr/share/elasticsearch/data
    mem_limit: 1g

  # ========== Kibana (可选) ==========
  kibana:
    image: kibana:8.13.0
    container_name: ecommerce-kibana
    ports:
      - "5601:5601"
    environment:
      ELASTICSEARCH_HOSTS: http://elasticsearch:9200
    depends_on:
      - elasticsearch
    profiles:
      - full   # 仅全量方案启动

volumes:
  mysql-data:
  redis-data:
  es-data:
```

**启动命令：**

```bash
# 完整版（含 ES）
docker compose -f docker/docker-compose.yml up -d

# 轻量版（不含 ES）
docker compose -f docker/docker-compose.yml up -d mysql redis rabbitmq nacos sentinel

# 查看启动日志
docker compose -f docker/docker-compose.yml logs -f

# 停止
docker compose -f docker/docker-compose.yml down
```

### 1.5 Nacos 初始化配置

启动后访问 http://localhost:8848/nacos（默认账号密码：nacos/nacos）

在 Nacos 中创建命名空间 `ecommerce-dev`，并添加以下公共配置：

**Data ID: `ecommerce-common.yml`**，Group: `DEFAULT_GROUP`

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ecommerce?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root123
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin123
    virtual-host: /

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
```

### 1.6 数据库表设计

**核心表清单（12 张）：**

```sql
-- ========================================
-- 1. 用户表
-- ========================================
CREATE TABLE `user` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '用户ID',
    `username`    VARCHAR(50)  NOT NULL COMMENT '用户名',
    `password`    VARCHAR(255) NOT NULL COMMENT '加密密码',
    `phone`       VARCHAR(20)  DEFAULT NULL COMMENT '手机号',
    `email`       VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
    `nickname`    VARCHAR(50)  DEFAULT NULL COMMENT '昵称',
    `avatar`      VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
    `status`      TINYINT      DEFAULT 1 COMMENT '状态: 1正常 0禁用',
    `deleted`     TINYINT      DEFAULT 0 COMMENT '逻辑删除',
    `create_time` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`),
    UNIQUE KEY `uk_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- ========================================
-- 2. 角色表
-- ========================================
CREATE TABLE `role` (
    `id`          BIGINT      NOT NULL AUTO_INCREMENT,
    `name`        VARCHAR(50) NOT NULL COMMENT '角色名称(ROLE_ADMIN/ROLE_USER)',
    `description` VARCHAR(255) DEFAULT NULL,
    `create_time` DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- ========================================
-- 3. 用户角色关联表
-- ========================================
CREATE TABLE `user_role` (
    `id`      BIGINT NOT NULL AUTO_INCREMENT,
    `user_id` BIGINT NOT NULL,
    `role_id` BIGINT NOT NULL,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_role` (`user_id`, `role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';

-- ========================================
-- 4. 权限表
-- ========================================
CREATE TABLE `permission` (
    `id`          BIGINT      NOT NULL AUTO_INCREMENT,
    `name`        VARCHAR(50) NOT NULL COMMENT '权限标识(如 product:list)',
    `description` VARCHAR(255) DEFAULT NULL,
    `create_time` DATETIME    DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权限表';

-- ========================================
-- 5. 角色权限关联表
-- ========================================
CREATE TABLE `role_permission` (
    `id`            BIGINT NOT NULL AUTO_INCREMENT,
    `role_id`       BIGINT NOT NULL,
    `permission_id` BIGINT NOT NULL,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_role_perm` (`role_id`, `permission_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色权限关联表';

-- ========================================
-- 6. 商品分类表
-- ========================================
CREATE TABLE `category` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT,
    `name`        VARCHAR(100) NOT NULL COMMENT '分类名称',
    `parent_id`   BIGINT       DEFAULT 0 COMMENT '父分类ID',
    `sort_order`  INT          DEFAULT 0 COMMENT '排序',
    `create_time` DATETIME     DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品分类表';

-- ========================================
-- 7. 商品表
-- ========================================
CREATE TABLE `product` (
    `id`          BIGINT         NOT NULL AUTO_INCREMENT,
    `name`        VARCHAR(200)   NOT NULL COMMENT '商品名称',
    `description` TEXT           COMMENT '商品描述',
    `category_id` BIGINT         NOT NULL COMMENT '分类ID',
    `price`       DECIMAL(10,2)  NOT NULL COMMENT '单价',
    `stock`       INT            NOT NULL DEFAULT 0 COMMENT '库存',
    `image`       VARCHAR(500)   DEFAULT NULL COMMENT '主图URL',
    `sales`       INT            DEFAULT 0 COMMENT '销量',
    `status`      TINYINT        DEFAULT 1 COMMENT '状态: 1上架 0下架',
    `deleted`     TINYINT        DEFAULT 0,
    `create_time` DATETIME       DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME       DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_category` (`category_id`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';

-- ========================================
-- 8. 商品 SKU 表（规格库存）
-- ========================================
CREATE TABLE `product_sku` (
    `id`          BIGINT         NOT NULL AUTO_INCREMENT,
    `product_id`  BIGINT         NOT NULL COMMENT '商品ID',
    `attrs`       VARCHAR(200)   NOT NULL COMMENT '规格属性(如 "颜色:黑,尺寸:M")',
    `price`       DECIMAL(10,2)  NOT NULL COMMENT 'SKU价格',
    `stock`       INT            NOT NULL DEFAULT 0 COMMENT 'SKU库存',
    `version`     INT            DEFAULT 0 COMMENT '乐观锁版本号',
    `create_time` DATETIME       DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME       DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_product` (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品SKU表';

-- ========================================
-- 9. 购物车表 (Redis 持久化，MySQL 做备份)
-- ========================================
CREATE TABLE `cart` (
    `id`          BIGINT NOT NULL AUTO_INCREMENT,
    `user_id`     BIGINT NOT NULL COMMENT '用户ID',
    `product_id`  BIGINT NOT NULL COMMENT '商品ID',
    `sku_id`      BIGINT DEFAULT NULL COMMENT 'SKU ID',
    `quantity`    INT    NOT NULL DEFAULT 1 COMMENT '数量',
    `selected`    TINYINT DEFAULT 1 COMMENT '是否选中',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_product` (`user_id`, `product_id`, `sku_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='购物车表(备份)';

-- ========================================
-- 10. 订单表
-- ========================================
CREATE TABLE `orders` (
    `id`                BIGINT         NOT NULL AUTO_INCREMENT,
    `order_no`          VARCHAR(32)    NOT NULL COMMENT '订单号(唯一)',
    `user_id`           BIGINT         NOT NULL COMMENT '用户ID',
    `total_amount`      DECIMAL(12,2)  NOT NULL COMMENT '订单总金额',
    `pay_amount`        DECIMAL(12,2)  NOT NULL COMMENT '实付金额',
    `status`            TINYINT        NOT NULL DEFAULT 0 COMMENT '状态: 0待支付 1已支付 2已发货 3已收货 4已完成 5已取消 6已退款',
    `pay_type`          TINYINT        DEFAULT NULL COMMENT '支付方式: 1微信 2支付宝 3模拟',
    `pay_time`          DATETIME       DEFAULT NULL COMMENT '支付时间',
    `consignee`         VARCHAR(50)    DEFAULT NULL COMMENT '收货人',
    `phone`             VARCHAR(20)    DEFAULT NULL COMMENT '收货电话',
    `address`           VARCHAR(255)   DEFAULT NULL COMMENT '收货地址',
    `remark`            VARCHAR(500)   DEFAULT NULL COMMENT '订单备注',
    `cancel_reason`     VARCHAR(255)   DEFAULT NULL COMMENT '取消原因',
    `deleted`           TINYINT        DEFAULT 0,
    `create_time`       DATETIME       DEFAULT CURRENT_TIMESTAMP,
    `update_time`       DATETIME       DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_order_no` (`order_no`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_status` (`status`),
    KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';

-- ========================================
-- 11. 订单明细表
-- ========================================
CREATE TABLE `order_item` (
    `id`           BIGINT         NOT NULL AUTO_INCREMENT,
    `order_no`     VARCHAR(32)    NOT NULL COMMENT '订单号',
    `product_id`   BIGINT         NOT NULL COMMENT '商品ID',
    `sku_id`       BIGINT         DEFAULT NULL,
    `product_name` VARCHAR(200)   NOT NULL COMMENT '商品名称',
    `product_image` VARCHAR(500)  DEFAULT NULL COMMENT '商品图片',
    `sku_attrs`    VARCHAR(200)   DEFAULT NULL COMMENT '购买时的SKU属性',
    `price`        DECIMAL(10,2)  NOT NULL COMMENT '购买单价',
    `quantity`     INT            NOT NULL COMMENT '购买数量',
    `subtotal`     DECIMAL(12,2)  NOT NULL COMMENT '小计金额',
    `create_time`  DATETIME       DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_order_no` (`order_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单明细表';

-- ========================================
-- 12. 支付流水表
-- ========================================
CREATE TABLE `payment` (
    `id`             BIGINT         NOT NULL AUTO_INCREMENT,
    `pay_no`         VARCHAR(32)    NOT NULL COMMENT '支付流水号',
    `order_no`       VARCHAR(32)    NOT NULL COMMENT '关联订单号',
    `user_id`        BIGINT         NOT NULL COMMENT '用户ID',
    `total_amount`   DECIMAL(12,2)  NOT NULL COMMENT '支付金额',
    `pay_type`       TINYINT        DEFAULT 1 COMMENT '支付方式: 1微信 2支付宝 3模拟',
    `status`         TINYINT        DEFAULT 0 COMMENT '状态: 0待支付 1支付成功 2支付失败 3已退款',
    `callback_time`  DATETIME       DEFAULT NULL COMMENT '回调时间',
    `create_time`    DATETIME       DEFAULT CURRENT_TIMESTAMP,
    `update_time`    DATETIME       DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_pay_no` (`pay_no`),
    KEY `idx_order_no` (`order_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='支付流水表';
```

---

## 阶段二：用户模块 + 安全体系（第 2 周）

### 2.1 模块结构

```
user-service/
├── src/main/java/.../user/
│   ├── controller/
│   │   ├── AuthController.java        # 登录/注册/刷新Token
│   │   └── UserController.java        # 用户信息管理
│   ├── service/
│   │   ├── UserService.java
│   │   └── impl/UserServiceImpl.java
│   ├── mapper/
│   │   ├── UserMapper.java
│   │   ├── RoleMapper.java
│   │   └── UserRoleMapper.java
│   ├── entity/
│   │   ├── User.java
│   │   ├── Role.java
│   │   └── UserRole.java
│   ├── dto/
│   │   ├── LoginRequest.java
│   │   ├── RegisterRequest.java
│   │   ├── LoginResponse.java
│   │   └── UserInfoVO.java
│   ├── security/
│   │   ├── JwtTokenProvider.java       # JWT 生成/验证
│   │   ├── JwtAuthenticationFilter.java # 请求拦截解析Token
│   │   ├── UserDetailsServiceImpl.java # Spring Security 用户加载
│   │   └── SecurityConfig.java         # Security 核心配置
│   └── UserApplication.java
```

### 2.2 JWT 双 Token 机制

```java
// ========== JwtTokenProvider.java ==========
@Component
public class JwtTokenProvider {

    @Value("${jwt.access-expiration:3600000}")    // 1小时
    private long accessExpiration;
    @Value("${jwt.refresh-expiration:604800000}") // 7天
    private long refreshExpiration;

    private final SecretKey secretKey;

    public JwtTokenProvider() {
        // 生产环境应该从配置中心读取
        this.secretKey = Keys.hmacShaKeyFor(
            "YourSuperSecretKeyForJWTTokenGenerationAtLeast256BitsLong!!"
                .getBytes(StandardCharsets.UTF_8)
        );
    }

    // 生成 Access Token
    public String generateAccessToken(Long userId, String username, Set<String> roles) {
        return Jwts.builder()
            .subject(userId.toString())
            .claim("username", username)
            .claim("roles", roles)
            .issuedAt(new Date())
            .expiration(new Date(System.currentTimeMillis() + accessExpiration))
            .signWith(secretKey)
            .compact();
    }

    // 生成 Refresh Token
    public String generateRefreshToken(Long userId) {
        return Jwts.builder()
            .subject(userId.toString())
            .claim("type", "refresh")
            .issuedAt(new Date())
            .expiration(new Date(System.currentTimeMillis() + refreshExpiration))
            .signWith(secretKey)
            .compact();
    }

    // 解析 Token
    public Claims parseToken(String token) {
        return Jwts.parser()
            .verifyWith(secretKey)
            .build()
            .parseSignedClaims(token)
            .getPayload();
    }

    // 验证 Token 是否可用
    public boolean validateToken(String token) {
        try {
            parseToken(token);
            return true;
        } catch (JwtException | IllegalArgumentException e) {
            return false;
        }
    }

    // 从 Token 提取用户 ID
    public Long getUserIdFromToken(String token) {
        return Long.parseLong(parseToken(token).getSubject());
    }
}
```

### 2.3 JWT 认证过滤器

```java
// ========== JwtAuthenticationFilter.java ==========
@Component
@RequiredArgsConstructor
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    private final JwtTokenProvider jwtTokenProvider;
    private final UserDetailsService userDetailsService;

    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                    HttpServletResponse response,
                                    FilterChain filterChain)
            throws ServletException, IOException {

        String token = resolveToken(request);

        if (token != null && jwtTokenProvider.validateToken(token)) {
            Long userId = jwtTokenProvider.getUserIdFromToken(token);
            UserDetails userDetails = userDetailsService.loadUserByUsername(userId.toString());

            UsernamePasswordAuthenticationToken authentication =
                new UsernamePasswordAuthenticationToken(
                    userDetails, null, userDetails.getAuthorities()
                );
            authentication.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
            SecurityContextHolder.getContext().setAuthentication(authentication);
        }

        filterChain.doFilter(request, response);
    }

    private String resolveToken(HttpServletRequest request) {
        String bearerToken = request.getHeader("Authorization");
        if (bearerToken != null && bearerToken.startsWith("Bearer ")) {
            return bearerToken.substring(7);
        }
        return null;
    }
}
```

### 2.4 Spring Security 配置

```java
// ========== SecurityConfig.java ==========
@Configuration
@EnableWebSecurity
@EnableMethodSecurity  // 开启 @PreAuthorize
@RequiredArgsConstructor
public class SecurityConfig {

    private final JwtAuthenticationFilter jwtAuthFilter;

    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
            .csrf(AbstractHttpConfigurer::disable)
            .sessionManagement(session ->
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**").permitAll()          // 登录注册开放
                .requestMatchers("/actuator/**").permitAll()          // 健康检查
                .requestMatchers("/api/admin/**").hasRole("ADMIN")    // 后台需管理员
                .anyRequest().authenticated()                         // 其余需认证
            )
            .addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class);

        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }

    @Bean
    public AuthenticationManager authenticationManager(
            AuthenticationConfiguration config) throws Exception {
        return config.getAuthenticationManager();
    }
}
```

### 2.5 登录/注册接口

```java
// ========== AuthController.java ==========
@RestController
@RequestMapping("/api/auth")
@RequiredArgsConstructor
public class AuthController {

    private final UserService userService;

    @PostMapping("/login")
    public Result<LoginResponse> login(@Valid @RequestBody LoginRequest request) {
        LoginResponse response = userService.login(request);
        return Result.success(response);
    }

    @PostMapping("/register")
    public Result<Void> register(@Valid @RequestBody RegisterRequest request) {
        userService.register(request);
        return Result.success();
    }

    @PostMapping("/refresh")
    public Result<LoginResponse> refresh(@RequestParam String refreshToken) {
        LoginResponse response = userService.refreshToken(refreshToken);
        return Result.success(response);
    }

    @PostMapping("/logout")
    public Result<Void> logout(@RequestHeader("Authorization") String token) {
        userService.logout(token);
        return Result.success();
    }
}
```

### 2.6 AOP 日志切面

```java
// ========== LogAspect.java ==========
@Aspect
@Component
@Slf4j
public class LogAspect {

    @Pointcut("execution(* com.ecommerce.*.controller.*.*(..))")
    public void controllerPointcut() {}

    @Around("controllerPointcut()")
    public Object logAround(ProceedingJoinPoint joinPoint) throws Throwable {
        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        String methodName = signature.getDeclaringTypeName() + "." + signature.getName();
        Object[] args = joinPoint.getArgs();

        log.info("[操作日志] 方法: {}, 参数: {}", methodName, JSON.toJSONString(args));
        long startTime = System.currentTimeMillis();

        Object result = joinPoint.proceed();

        long elapsed = System.currentTimeMillis() - startTime;
        log.info("[操作日志] 方法: {}, 耗时: {}ms, 结果: {}",
            methodName, elapsed, JSON.toJSONString(result));

        return result;
    }
}
```

### 2.7 统一异常处理

```java
// ========== GlobalExceptionHandler.java (放在 common 模块) ==========
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValidation(MethodArgumentNotValidException e) {
        String msg = e.getBindingResult().getFieldErrors().stream()
            .map(err -> err.getField() + ": " + err.getDefaultMessage())
            .collect(Collectors.joining(", "));
        return Result.error(400, msg);
    }

    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusiness(BusinessException e) {
        log.warn("业务异常: {}", e.getMessage());
        return Result.error(e.getCode(), e.getMessage());
    }

    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        log.error("系统异常", e);
        return Result.error(500, "服务器内部错误");
    }
}
```

---

## 阶段三：商品模块 + 缓存体系（第 3 周）

### 3.1 商品 CRUD（MyBatis-Plus）

```java
// ========== ProductController.java ==========
@RestController
@RequestMapping("/api/products")
@RequiredArgsConstructor
public class ProductController {

    private final ProductService productService;

    @GetMapping("/{id}")
    public Result<ProductVO> getProduct(@PathVariable Long id) {
        return Result.success(productService.getProductDetail(id));
    }

    @GetMapping("/page")
    public Result<PageResult<ProductVO>> pageProducts(
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "10") int size,
            ProductQuery query) {
        return Result.success(productService.pageProducts(page, size, query));
    }

    @PostMapping
    @PreAuthorize("hasPermission('product:create')")
    public Result<Void> createProduct(@Valid @RequestBody ProductCreateRequest request) {
        productService.createProduct(request);
        return Result.success();
    }

    @PutMapping("/{id}")
    @PreAuthorize("hasPermission('product:update')")
    public Result<Void> updateProduct(@PathVariable Long id,
                                      @Valid @RequestBody ProductUpdateRequest request) {
        productService.updateProduct(id, request);
        return Result.success();
    }

    @DeleteMapping("/{id}")
    @PreAuthorize("hasPermission('product:delete')")
    public Result<Void> deleteProduct(@PathVariable Long id) {
        productService.deleteProduct(id);
        return Result.success();
    }
}
```

### 3.2 缓存三大问题完整方案

```java
// ========== ProductCacheService.java ==========
@Service
@Slf4j
@RequiredArgsConstructor
public class ProductCacheService {

    private final StringRedisTemplate redisTemplate;
    private final ProductMapper productMapper;
    private final RedissonClient redissonClient;

    // 布隆过滤器（缓存穿透防护）
    private final BloomFilter<Long> bloomFilter = BloomFilter.create(
        Funnels.longFunnel(),  // 预期插入数量
        100_000,               // 预期插入数量
        0.01                   // 误判率
    );

    @PostConstruct
    public void initBloomFilter() {
        List<Long> allIds = productMapper.selectAllIds();
        allIds.forEach(bloomFilter::put);
        log.info("布隆过滤器初始化完成，加载 {} 个商品ID", allIds.size());
    }

    // ========== 商品详情查询（含缓存穿透/击穿/雪崩防护） ==========
    public ProductVO getProductDetail(Long productId) {
        String cacheKey = "product:detail:" + productId;

        // 1️⃣ 防缓存穿透：布隆过滤器前置拦截
        if (!bloomFilter.mightContain(productId)) {
            log.warn("布隆过滤器拦截非法商品ID: {}", productId);
            return null;
        }

        // 2️⃣ 查缓存
        String cachedJson = redisTemplate.opsForValue().get(cacheKey);
        if (StrUtil.isNotBlank(cachedJson)) {
            return JSON.parseObject(cachedJson, ProductVO.class);
        }

        // 3️⃣ 防缓存击穿：互斥锁，只让一个线程查 DB
        String lockKey = "lock:product:" + productId;
        RLock lock = redissonClient.getLock(lockKey);
        try {
            // 尝试加锁，等待 2 秒，锁 10 秒自动释放
            if (lock.tryLock(2, 10, TimeUnit.SECONDS)) {
                // 双重检查 — 锁等待期间可能已被其他线程写入
                cachedJson = redisTemplate.opsForValue().get(cacheKey);
                if (StrUtil.isNotBlank(cachedJson)) {
                    return JSON.parseObject(cachedJson, ProductVO.class);
                }

                // 查数据库
                Product product = productMapper.selectById(productId);
                if (product == null) {
                    // ⚠️ 防穿透：缓存空值（短过期时间）
                    redisTemplate.opsForValue().set(cacheKey, "", 30, TimeUnit.SECONDS);
                    return null;
                }

                ProductVO vo = convertToVO(product);

                // 4️⃣ 防缓存雪崩：随机过期时间（基础值 + 随机偏移）
                long baseExpire = 3600; // 1小时
                long randomExpire = baseExpire + ThreadLocalRandom.current().nextLong(1800);
                redisTemplate.opsForValue().set(
                    cacheKey, JSON.toJSONString(vo), randomExpire, TimeUnit.SECONDS);

                return vo;
            } else {
                // 没拿到锁，返回降级数据
                log.warn("获取锁超时，返回降级数据: productId={}", productId);
                return getFallbackProduct(productId);
            }
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            return getFallbackProduct(productId);
        } finally {
            if (lock.isHeldByCurrentThread()) {
                lock.unlock();
            }
        }
    }

    // 降级方案：从 DB 直查（不经过缓存）
    private ProductVO getFallbackProduct(Long productId) {
        Product product = productMapper.selectById(productId);
        return product != null ? convertToVO(product) : null;
    }
}
```

### 3.3 Redis Lua 库存扣减

```lua
-- ========== stock_decrement.lua ==========
-- KEYS[1]: 库存 key (e.g., "stock:sku:1001")
-- ARGV[1]: 扣减数量
-- ARGV[2]: 版本号（乐观锁）

local stock = redis.call('GET', KEYS[1])
if not stock then
    return -1  -- 库存不存在
end

local currentStock = tonumber(stock)
local quantity = tonumber(ARGV[1])

if currentStock < quantity then
    return 0   -- 库存不足
end

redis.call('DECRBY', KEYS[1], quantity)
return 1       -- 扣减成功
```

```java
// ========== InventoryService.java ==========
public boolean decrementStock(Long skuId, int quantity) {
    // 执行 Lua 脚本保证原子性
    DefaultRedisScript<Long> script = new DefaultRedisScript<>();
    script.setScriptText(LUA_SCRIPT);
    script.setResultType(Long.class);

    List<String> keys = Collections.singletonList("stock:sku:" + skuId);
    Long result = redisTemplate.execute(script, keys, String.valueOf(quantity));

    if (result == null || result <= 0) {
        return false;
    }

    // 异步发送消息到 MQ 同步 MySQL
    rabbitTemplate.convertAndSend(
        "exchange.stock", "routing.stock.update",
        new StockChangeMessage(skuId, -quantity));

    return true;
}
```

### 3.4 热门商品排行榜（Redis ZSet）

```java
// ========== HotProductService.java ==========
@Service
@RequiredArgsConstructor
public class HotProductService {

    private static final String HOT_KEY = "hot:products";
    private static final int TOP_N = 20;

    private final StringRedisTemplate redisTemplate;

    // 用户浏览商品时增加热度
    public void recordView(Long productId) {
        redisTemplate.opsForZSet().incrementScore(HOT_KEY, productId.toString(), 1);
    }

    // 获取热榜 Top-N
    public List<Long> getHotProducts() {
        Set<String> topIds = redisTemplate.opsForZSet()
            .reverseRangeByScore(HOT_KEY, 0, Double.MAX_VALUE, 0, TOP_N);
        if (topIds == null || topIds.isEmpty()) return Collections.emptyList();
        return topIds.stream().map(Long::valueOf).collect(Collectors.toList());
    }
}
```

---

## 阶段四：购物车 + 订单核心链路（第 4 周）

### 4.1 购物车（Redis 实现）

```java
// ========== CartService.java ==========
@Service
@RequiredArgsConstructor
public class CartService {

    private final StringRedisTemplate redisTemplate;
    private final ProductService productService;

    private static final String CART_PREFIX = "cart:user:";

    // ========== 添加购物车 ==========
    public void addItem(Long userId, Long productId, Long skuId, int quantity) {
        String cartKey = CART_PREFIX + userId;
        String itemKey = productId + ":" + skuId;

        // 使用 Hash 存储，field = productId:skuId, value = JSON{quantity, ...}
        String existing = (String) redisTemplate.opsForHash().get(cartKey, itemKey);
        if (existing != null) {
            CartItem item = JSON.parseObject(existing, CartItem.class);
            item.setQuantity(item.getQuantity() + quantity);
            redisTemplate.opsForHash().put(cartKey, itemKey, JSON.toJSONString(item));
        } else {
            CartItem item = new CartItem(userId, productId, skuId, quantity);
            redisTemplate.opsForHash().put(cartKey, itemKey, JSON.toJSONString(item));
        }
    }

    // ========== 获取购物车列表 ==========
    public List<CartItemVO> getCartItems(Long userId) {
        String cartKey = CART_PREFIX + userId;
        Map<Object, Object> entries = redisTemplate.opsForHash().entries(cartKey);

        return entries.values().stream()
            .map(v -> JSON.parseObject((String) v, CartItem.class))
            .map(this::enrichProductInfo)  // 填充商品名称、价格、图片
            .sorted(Comparator.comparing(CartItemVO::getUpdateTime).reversed())
            .collect(Collectors.toList());
    }

    // ========== 合并购物车（游客登录后合并） ==========
    public void mergeCart(Long userId, List<CartItem> tempItems) {
        tempItems.forEach(item -> addItem(userId, item.getProductId(),
            item.getSkuId(), item.getQuantity()));
    }
}
```

### 4.2 下单核心流程

```java
// ========== OrderService.java ==========
@Service
@RequiredArgsConstructor
@Slf4j
public class OrderServiceImpl implements OrderService {

    private final OrderMapper orderMapper;
    private final OrderItemMapper orderItemMapper;
    private final InventoryService inventoryService;
    private final CartService cartService;
    private final StringRedisTemplate redisTemplate;
    private final RabbitTemplate rabbitTemplate;

    @Override
    @Transactional(rollbackFor = Exception.class)
    public OrderVO createOrder(CreateOrderRequest request) {
        Long userId = CurrentUserHolder.getUserId();

        // 1️⃣ 幂等性校验：防止重复提交
        String idempotentKey = "idempotent:order:" + userId + ":" + request.getIdempotentToken();
        Boolean success = redisTemplate.opsForValue()
            .setIfAbsent(idempotentKey, "1", 30, TimeUnit.SECONDS);
        if (Boolean.FALSE.equals(success)) {
            throw new BusinessException(400, "订单已提交，请勿重复操作");
        }

        // 2️⃣ 库存预扣（Redis Lua 原子操作）
        List<OrderItemRequest> items = request.getItems();
        for (OrderItemRequest item : items) {
            boolean ok = inventoryService.decrementStock(item.getSkuId(), item.getQuantity());
            if (!ok) {
                throw new BusinessException(400, "商品库存不足: skuId=" + item.getSkuId());
            }
        }

        // 3️⃣ 生成订单号
        String orderNo = generateOrderNo();

        try {
            // 4️⃣ 保存订单
            Orders order = buildOrder(userId, orderNo, request);
            orderMapper.insert(order);

            List<OrderItem> orderItems = buildOrderItems(orderNo, items);
            orderItemMapper.insertBatch(orderItems);  // MyBatis-Plus 批量插入

            // 5️⃣ 清空购物车中已下单的商品
            cartService.removeItems(userId, items);

            // 6️⃣ 发送延迟消息：30 分钟后检查支付状态
            rabbitTemplate.convertAndSend(
                "exchange.order.delay",
                "routing.order.delay",
                orderNo,
                message -> {
                    message.getMessageProperties().setDelay(30 * 60 * 1000); // 30 分钟
                    return message;
                });

            // 7️⃣ 发送消息同步 ES（如果有 ES）
            rabbitTemplate.convertAndSend("exchange.order.event",
                "routing.order.created", orderNo);

            return convertToVO(order, orderItems);

        } catch (Exception e) {
            log.error("下单失败，回滚库存: orderNo={}", orderNo, e);
            // 库存回滚（发送 MQ 消息异步回滚）
            for (OrderItemRequest item : items) {
                rabbitTemplate.convertAndSend("exchange.stock",
                    "routing.stock.rollback",
                    new StockChangeMessage(item.getSkuId(), item.getQuantity()));
            }
            throw new BusinessException(500, "下单失败，请重试");
        }
    }

    private String generateOrderNo() {
        // 订单号 = 日期 + 用户ID后4位 + 雪花算法
        LocalDate now = LocalDate.now();
        String dateStr = now.format(DateTimeFormatter.ofPattern("yyyyMMdd"));
        Long userId = CurrentUserHolder.getUserId();
        String userIdSuffix = String.format("%04d", userId % 10000);
        long snowflakeId = IdUtil.getSnowflake().nextId();
        return dateStr + userIdSuffix + (snowflakeId % 1000000);
    }
}
```

### 4.3 订单状态机

```java
// ========== 订单状态枚举 ==========
public enum OrderStatus {
    PENDING_PAYMENT(0, "待支付"),
    PAID(1, "已支付"),
    SHIPPED(2, "已发货"),
    RECEIVED(3, "已收货"),
    COMPLETED(4, "已完成"),
    CANCELLED(5, "已取消"),
    REFUNDED(6, "已退款");

    private final int code;
    private final String desc;
}

// ========== 状态转换校验 ==========
@Component
public class OrderStateMachine {

    // 定义合法的状态转换
    private static final Map<OrderStatus, Set<OrderStatus>> TRANSITIONS = new HashMap<>();

    static {
        TRANSITIONS.put(PENDING_PAYMENT, Set.of(PAID, CANCELLED));
        TRANSITIONS.put(PAID, Set.of(SHIPPED, REFUNDED, CANCELLED));
        TRANSITIONS.put(SHIPPED, Set.of(RECEIVED));
        TRANSITIONS.put(RECEIVED, Set.of(COMPLETED));
        TRANSITIONS.put(COMPLETED, Set.of());
        TRANSITIONS.put(CANCELLED, Set.of());
        TRANSITIONS.put(REFUNDED, Set.of());
    }

    public boolean canTransition(OrderStatus from, OrderStatus to) {
        Set<OrderStatus> allowed = TRANSITIONS.get(from);
        return allowed != null && allowed.contains(to);
    }
}
```

### 4.4 RabbitMQ 延迟队列（超时取消订单）

```java
// ========== RabbitMQ 配置 ==========
@Configuration
public class RabbitMQConfig {

    // ========== 订单延迟队列 ==========
    @Bean
    public Queue orderDelayQueue() {
        return QueueBuilder.durable("queue.order.delay")
            .withArgument("x-dead-letter-exchange", "exchange.order.dlx")
            .withArgument("x-dead-letter-routing-key", "routing.order.dlx")
            .withArgument("x-message-ttl", 30 * 60 * 1000)  // 30 分钟 TTL
            .build();
    }

    @Bean
    public Queue orderDlxQueue() {
        return QueueBuilder.durable("queue.order.dlx").build();
    }

    @Bean
    public DirectExchange orderDelayExchange() {
        return new DirectExchange("exchange.order.delay");
    }

    @Bean
    public DirectExchange orderDlxExchange() {
        return new DirectExchange("exchange.order.dlx");
    }

    @Bean
    public Binding orderDelayBinding() {
        return BindingBuilder.bind(orderDelayQueue())
            .to(orderDelayExchange()).with("routing.order.delay");
    }

    @Bean
    public Binding orderDlxBinding() {
        return BindingBuilder.bind(orderDlxQueue())
            .to(orderDlxExchange()).with("routing.order.dlx");
    }

    // ========== 库存同步队列 ==========
    @Bean
    public Queue stockUpdateQueue() {
        return new Queue("queue.stock.update", true);
    }

    @Bean
    public DirectExchange stockExchange() {
        return new DirectExchange("exchange.stock");
    }

    @Bean
    public Binding stockBinding() {
        return BindingBuilder.bind(stockUpdateQueue())
            .to(stockExchange()).with("routing.stock.update");
    }
}
```

```java
// ========== 延迟队列消费者（超时取消订单） ==========
@Component
@Slf4j
@RequiredArgsConstructor
public class OrderDelayConsumer {

    private final OrderMapper orderMapper;
    private final OrderStateMachine stateMachine;
    private final InventoryService inventoryService;

    @RabbitListener(queues = "queue.order.dlx")
    public void handleOrderTimeout(String orderNo) {
        log.info("接收到订单超时消息: orderNo={}", orderNo);

        Orders order = orderMapper.selectByOrderNo(orderNo);
        if (order == null) return;

        // 只有待支付状态才取消
        if (order.getStatus() == OrderStatus.PENDING_PAYMENT.getCode()
            && stateMachine.canTransition(
                OrderStatus.PENDING_PAYMENT, OrderStatus.CANCELLED)) {

            order.setStatus(OrderStatus.CANCELLED.getCode());
            order.setCancelReason("超时未支付，系统自动取消");
            orderMapper.updateById(order);

            // 回滚库存
            List<OrderItem> items = orderItemMapper.selectByOrderNo(orderNo);
            items.forEach(item -> inventoryService.rollbackStock(
                item.getSkuId(), item.getQuantity()));

            log.info("订单已超时取消: orderNo={}", orderNo);
        }
    }
}
```

### 4.5 分布式事务（MQ 最终一致性）

```java
// ========== 本地消息表 + MQ 重试 ==========
// 方案概述：
// 1. 下单时先插入本地消息表（状态=待发送）
// 2. 定时任务扫描待发送消息并发送到 MQ
// 3. MQ 消费者处理成功后回调修改消息状态
// 4. 达到最大重试次数仍未成功 → 人工介入

// 本地消息表 SQL
CREATE TABLE `order_message` (
    `id`            BIGINT       NOT NULL AUTO_INCREMENT,
    `order_no`      VARCHAR(32)  NOT NULL,
    `message_body`  TEXT         NOT NULL COMMENT '消息体(JSON)',
    `exchange`      VARCHAR(100) NOT NULL COMMENT '目标交换机',
    `routing_key`   VARCHAR(100) NOT NULL COMMENT '路由键',
    `status`        TINYINT      DEFAULT 0 COMMENT '状态: 0待发送 1已发送 2已确认 3已失败',
    `retry_count`   INT          DEFAULT 0 COMMENT '重试次数',
    `max_retry`     INT          DEFAULT 3 COMMENT '最大重试次数',
    `create_time`   DATETIME     DEFAULT CURRENT_TIMESTAMP,
    `update_time`   DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_status` (`status`),
    KEY `idx_order_no` (`order_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='本地消息表';

// ========== 定时任务：扫描并重发消息 ==========
@Component
@Slf4j
@RequiredArgsConstructor
public class MessageRetryTask {

    private final OrderMessageMapper messageMapper;
    private final RabbitTemplate rabbitTemplate;

    @Scheduled(fixedDelay = 10_000) // 每 10 秒执行一次
    @Transactional
    public void retryPendingMessages() {
        List<OrderMessage> pendingMessages = messageMapper.selectList(
            new LambdaQueryWrapper<OrderMessage>()
                .eq(OrderMessage::getStatus, 0)
                .lt(OrderMessage::getRetryCount, OrderMessage::getMaxRetry)
                .last("LIMIT 50")
        );

        for (OrderMessage msg : pendingMessages) {
            try {
                rabbitTemplate.convertAndSend(
                    msg.getExchange(), msg.getRoutingKey(), msg.getMessageBody());
                msg.setStatus(1); // 已发送
                log.info("消息重发成功: id={}, orderNo={}", msg.getId(), msg.getOrderNo());
            } catch (Exception e) {
                msg.setRetryCount(msg.getRetryCount() + 1);
                if (msg.getRetryCount() >= msg.getMaxRetry()) {
                    msg.setStatus(3); // 已失败
                    log.error("消息重发已达最大次数: id={}", msg.getId());
                }
                log.warn("消息重发失败: id={}, error={}", msg.getId(), e.getMessage());
            }
            messageMapper.updateById(msg);
        }
    }
}
```

---

## 阶段五：支付 + 搜索 + 管理后台（第 5 周）

### 5.1 模拟支付

```java
// ========== PaymentService.java（模拟支付） ==========
@Service
@RequiredArgsConstructor
@Slf4j
public class PaymentServiceImpl implements PaymentService {

    private final PaymentMapper paymentMapper;
    private final OrderMapper orderMapper;
    private final RabbitTemplate rabbitTemplate;
    private final StringRedisTemplate redisTemplate;

    @Override
    @Transactional
    public PayResponse pay(PayRequest request) {
        // 1️⃣ 校验订单状态
        Orders order = orderMapper.selectByOrderNo(request.getOrderNo());
        if (order == null) {
            throw new BusinessException(400, "订单不存在");
        }
        if (order.getStatus() != OrderStatus.PENDING_PAYMENT.getCode()) {
            throw new BusinessException(400, "订单状态异常，无法支付");
        }

        // 2️⃣ 生成支付流水
        String payNo = "PAY" + IdUtil.getSnowflake().nextId();
        Payment payment = new Payment();
        payment.setPayNo(payNo);
        payment.setOrderNo(request.getOrderNo());
        payment.setUserId(order.getUserId());
        payment.setTotalAmount(order.getPayAmount());
        payment.setPayType(request.getPayType());
        payment.setStatus(0); // 待支付
        paymentMapper.insert(payment);

        // 3️⃣ 模拟支付（延时 1~3 秒模拟异步回调）
        simulatePaymentAsync(payNo, request.getOrderNo());

        return new PayResponse(payNo, "支付处理中");
    }

    // 模拟异步支付回调
    private void simulatePaymentAsync(String payNo, String orderNo) {
        CompletableFuture.runAsync(() -> {
            try {
                // 模拟支付网关处理时间
                Thread.sleep(ThreadLocalRandom.current().nextLong(1000, 3000));

                // 模拟 90% 成功率
                boolean success = ThreadLocalRandom.current().nextDouble() < 0.9;

                if (success) {
                    handlePaymentSuccess(payNo, orderNo);
                } else {
                    handlePaymentFailure(payNo, orderNo, "模拟支付失败");
                }
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
            }
        });
    }

    @Transactional
    public void handlePaymentSuccess(String payNo, String orderNo) {
        // 更新支付流水
        Payment payment = paymentMapper.selectByPayNo(payNo);
        payment.setStatus(1); // 支付成功
        payment.setCallbackTime(LocalDateTime.now());
        paymentMapper.updateById(payment);

        // 更新订单状态
        Orders order = orderMapper.selectByOrderNo(orderNo);
        order.setStatus(OrderStatus.PAID.getCode());
        order.setPayTime(LocalDateTime.now());
        orderMapper.updateById(order);

        // 发送支付成功消息
        rabbitTemplate.convertAndSend("exchange.payment",
            "routing.payment.success", orderNo);

        log.info("支付成功: payNo={}, orderNo={}", payNo, orderNo);
    }
}
```

### 5.2 接口幂等性防重

```java
// ========== IdempotentUtil.java ==========
@Component
@RequiredArgsConstructor
public class IdempotentUtil {

    private final StringRedisTemplate redisTemplate;

    // 生成幂等 Token
    public String generateToken(String businessType, Long userId) {
        String token = UUID.randomUUID().toString().replace("-", "");
        String key = "idempotent:" + businessType + ":" + userId;
        redisTemplate.opsForValue().set(key + ":" + token, "1", 30, TimeUnit.MINUTES);
        return token;
    }

    // 校验并消费 Token
    public boolean consumeToken(String businessType, Long userId, String token) {
        String key = "idempotent:" + businessType + ":" + userId + ":" + token;
        Boolean deleted = redisTemplate.delete(key);
        return Boolean.TRUE.equals(deleted);
    }
}
```

### 5.3 前端 Vue 3 管理后台

```bash
# ========== 初始化 ==========
cd frontend
npm create vite@latest . -- --template vue-ts
npm install
npm install element-plus axios vue-router@4 pinia echarts vue-echarts

# ========== 目录结构 ==========
# frontend/
# ├── src/
# │   ├── api/           # API 接口封装
# │   ├── router/        # 路由配置
# │   ├── stores/        # Pinia 状态管理
# │   ├── views/         # 页面组件
# │   │   ├── login/     # 登录页
# │   │   ├── dashboard/ # 数据看板
# │   │   ├── product/   # 商品管理
# │   │   ├── order/     # 订单管理
# │   │   └── user/      # 用户管理
# │   ├── components/    # 公共组件
# │   ├── utils/         # 工具函数（axios 封装等）
# │   ├── App.vue
# │   └── main.ts
# ├── vite.config.ts
# └── package.json
```

**vite.config.ts 代理配置：**

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',  // Gateway 端口
        changeOrigin: true
      }
    }
  }
})
```

**axios 封装示例：**

```typescript
// src/utils/request.ts
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from '@/router'

const request = axios.create({
  baseURL: '/api',
  timeout: 15000
})

// 请求拦截器：自动携带 Token
request.interceptors.request.use(config => {
  const token = localStorage.getItem('accessToken')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器：统一错误处理 + Token 自动刷新
request.interceptors.response.use(
  response => {
    const { code, message, data } = response.data
    if (code === 200) return data
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  async error => {
    if (error.response?.status === 401) {
      // Token 过期，尝试刷新
      const refreshToken = localStorage.getItem('refreshToken')
      if (refreshToken) {
        try {
          const res = await axios.post('/api/auth/refresh', null, {
            params: { refreshToken }
          })
          localStorage.setItem('accessToken', res.data.data.accessToken)
          // 重试原请求
          error.config.headers.Authorization =
            `Bearer ${res.data.data.accessToken}`
          return request(error.config)
        } catch {
          // 刷新失败，跳登录
          localStorage.clear()
          router.push('/login')
        }
      }
    }
    ElMessage.error('网络错误')
    return Promise.reject(error)
  }
)

export default request
```

### 5.4 数据看板（ECharts）

```vue
<!-- src/views/dashboard/Dashboard.vue -->
<template>
  <div class="dashboard">
    <el-row :gutter="20">
      <el-col :span="6" v-for="card in statCards" :key="card.title">
        <el-card>
          <div class="stat-value">{{ card.value }}</div>
          <div class="stat-title">{{ card.title }}</div>
          <div class="stat-change" :class="card.trend">
            {{ card.change }} 较昨日
          </div>
        </el-card>
      </el-col>
    </el-row>

    <el-row :gutter="20" style="margin-top: 20px">
      <el-col :span="16">
        <el-card>
          <template #header>订单趋势</template>
          <v-chart :option="orderChartOption" style="height: 350px" />
        </el-card>
      </el-col>
      <el-col :span="8">
        <el-card>
          <template #header>商品分类占比</template>
          <v-chart :option="categoryChartOption" style="height: 350px" />
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script setup lang="ts">
import VChart from 'vue-echarts'
import { use } from 'echarts/core'
import { CanvasRenderer } from 'echarts/renderers'
import { LineChart, PieChart } from 'echarts/charts'
import { TitleComponent, TooltipComponent, LegendComponent } from 'echarts/components'

use([CanvasRenderer, LineChart, PieChart, TitleComponent, TooltipComponent, LegendComponent])

// ... 图表 option 配置完整代码见附件
</script>
```

---

## 阶段六：集成测试 + 压测 + 部署（第 6 周）

### 6.1 单元测试

```java
// ========== OrderServiceTest.java ==========
@SpringBootTest
@Transactional  // 测试自动回滚
class OrderServiceTest {

    @Autowired
    private OrderService orderService;

    @Test
    @DisplayName("下单 - 正常流程")
    void testCreateOrder_Success() {
        CreateOrderRequest request = new CreateOrderRequest();
        request.setUserId(1L);
        request.setItems(List.of(new OrderItemRequest(1L, 1L, 2)));

        OrderVO order = orderService.createOrder(request);

        assertNotNull(order);
        assertNotNull(order.getOrderNo());
        assertEquals(OrderStatus.PENDING_PAYMENT.getCode(), order.getStatus());
    }

    @Test
    @DisplayName("下单 - 库存不足")
    void testCreateOrder_InsufficientStock() {
        CreateOrderRequest request = new CreateOrderRequest();
        request.setItems(List.of(new OrderItemRequest(1L, 1L, 99999)));

        assertThrows(BusinessException.class, () -> orderService.createOrder(request));
    }

    @Test
    @DisplayName("下单 - 幂等性校验")
    void testCreateOrder_Idempotent() {
        CreateOrderRequest request = new CreateOrderRequest();
        request.setIdempotentToken("test-token-001");
        request.setItems(List.of(new OrderItemRequest(1L, 1L, 1)));

        orderService.createOrder(request);
        // 相同 Token 再次提交应抛异常
        assertThrows(BusinessException.class, () -> orderService.createOrder(request));
    }
}
```

### 6.2 JMeter 压测方案

**测试场景与目标：**

| 场景 | 并发数 | 目标 TPS | 施压时间 | 备注 |
|:----|:------:|:--------:|:--------:|:----|
| 商品详情查询 | 200 | > 2000 | 5 min | 测试缓存效果 |
| 登录接口 | 200 | > 500 | 3 min | JWT 生成性能 |
| 下单流程 | 100 | > 100 | 5 min | 含库存扣减 + MQ |
| 混合场景 | 200/500/1000 | - | 10 min | 综合压测 |

**JMeter 命令行执行：**

```bash
# 非 GUI 模式压测
jmeter -n -t test-plan/ecommerce-stress-test.jmx \
       -l test-plan/results/result-200.jtl \
       -e -o test-plan/report/200/

# 生成 HTML 报告
jmeter -g test-plan/results/result-200.jtl -o test-plan/report/200/
```

**Sentinel 降级验证（压测时同步验证）：**

```bash
# 在 Java 代码中触发限流：
# 1. Sentinel Dashboard 设置 QPS 阈值 = 100
# 2. JMeter 以 200 并发施压
# 3. 观察 Dashboard 中 blocked QPS 曲线上升
# 4. 验证被限流的请求走 Fallback 返回提示信息
```

### 6.3 Sentinel 限流配置

```yaml
# application.yml 中集成 Sentinel
spring:
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8858
      eager: true
```

```java
// ========== 限流切面 ==========
@Aspect
@Component
public class RateLimitAspect {

    @Around("@annotation(rateLimit)")
    public Object around(ProceedingJoinPoint joinPoint, RateLimit rateLimit) throws Throwable {
        // 获取资源名（方法全限定名）
        MethodSignature signature = (MethodSignature) joinPoint.getSignature();
        String resourceName = signature.getDeclaringTypeName() + "." + signature.getName();

        Entry entry = null;
        try {
            entry = SphU.entry(resourceName, ResourceTypeConstants.COMMON_WEB);
            return joinPoint.proceed();
        } catch (BlockException e) {
            // 被限流后的降级处理
            return Result.error(429, "系统繁忙，请稍后重试");
        } finally {
            if (entry != null) {
                entry.exit();
            }
        }
    }
}

// ========== 自定义注解 ==========
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RateLimit {
    String value() default "";       // 资源名称
    int limit() default 100;         // QPS 阈值
    String fallback() default "";    // 降级方法
}
```

### 6.4 Docker 应用容器化

**每个服务模块的 Dockerfile（以 order-service 为例）：**

```dockerfile
# order-service/Dockerfile
FROM eclipse-temurin:17-jre-alpine

WORKDIR /app

COPY build/libs/order-service-1.0.0.jar app.jar

EXPOSE 8084

ENTRYPOINT ["java", "-jar", "app.jar"]
```

**根构建脚本统一打包：**

```bash
# 构建所有模块
./gradlew clean build -x test

# 构建 Docker 镜像
docker build -t ecommerce/order-service:latest -f order-service/Dockerfile order-service/
```

**docker-compose.app.yml（应用编排）：**

```yaml
# docker/docker-compose.app.yml
version: "3.8"

services:
  gateway:
    image: ecommerce/gateway:latest
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    depends_on:
      - nacos
      - user-service
      - product-service

  user-service:
    image: ecommerce/user-service:latest
    ports:
      - "8081:8081"
    environment:
      SPRING_PROFILES_ACTIVE: docker

  product-service:
    image: ecommerce/product-service:latest
    ports:
      - "8082:8082"
    environment:
      SPRING_PROFILES_ACTIVE: docker

  # ... 其他服务类似
```

### 6.5 GitHub Actions CI

```yaml
# .github/workflows/ci.yml
name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root123
          MYSQL_DATABASE: ecommerce
        ports:
          - 3306:3306

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build with Gradle
        run: ./gradlew build -x test

      - name: Run tests
        run: ./gradlew test

      - name: Build Docker images
        run: |
          for service in gateway user-service product-service order-service; do
            docker build -t ecommerce/$service:${{ github.sha }} -f $service/Dockerfile $service/
          done
```

---

## 阶段七：答辩准备

### 7.1 答辩 PPT 大纲

| 章节 | 内容 | 页数 |
|:----|------|:----:|
| 项目背景 | 电商系统痛点、设计目标 | 1~2 |
| 技术选型 | 技术栈全景图、选型对比（为什么用 RabbitMQ 而不用 Kafka 等） | 2~3 |
| 系统架构 | 架构图、模块划分、核心交互流程 | 2~3 |
| **技术深度展示** | 4 个核心技术点各 1~2 页 | **4~8** |
| 压测结果 | JMeter 压测数据、TPS 对比（有缓存 vs 无缓存） | 2~3 |
| 部署方案 | Docker Compose 架构图 | 1~2 |
| 总结与展望 | 项目收获、可优化方向 | 1 |

### 7.2 答辩核心技术点（重点展示）

```
1️⃣ 缓存三大问题（缓存穿透/击穿/雪崩）
   - 带布隆过滤器 + 互斥锁 + 随机过期时间的完整方案
   - 压测数据对比：有缓存 QPS 3000+ vs 无缓存 QPS 200+

2️⃣ 分布式锁与库存扣减
   - Redis Lua 脚本保证库存扣减原子性
   - Redisson 看门狗自动续期
   - JMeter 验证 100 并发下无超卖

3️⃣ 分布式事务（MQ 最终一致性）
   - 本地消息表 + 定时重试
   - 面试必问：为什么不用 Seata？→ 答：MQ 方案更轻量，适合电商高并发场景

4️⃣ 接口幂等性
   - Redis Token + 数据库唯一约束双重保障
   - JMeter 并发 100 个相同请求验证只生成 1 个订单
```

### 7.3 面试可能问到的问题

```text
Q: 你们的缓存和数据库如何保证一致性？
A: 先更新数据库 → 再删除缓存（Cache Aside Pattern）。
   极端情况采用 MQ 异步重试删除。

Q: RabbitMQ 如何保证消息不丢失？
A: 生产者：Confirm 机制 → 消费者：手动 ACK →
   持久化（queue durable + message persistent）。

Q: 分布式锁用 Redis 还是 ZooKeeper？
A: 本项目高并发场景选 Redis（性能优先）。
   ZK 更适合强一致性场景（如分布式选举）。

Q: 如果 Redis 宕机了怎么办？
A: Redis 哨兵模式自动切换，缓存不可用期间
   请求直接走数据库（降级），恢复后重建缓存。

Q: 你们的系统能抗住多大并发？
A: 单机单实例，配合 Redis 缓存 + MQ 削峰，
   实测 1000 并发下单 TPS 约 500+。
   水平扩展后吞吐量线性增长。
```

### 7.4 加分展示建议

- **录制演示视频**：在答辩现场展示系统运行，比静态截图说服力强得多
- **压测动图**：用 GIF 展示 JMeter 压测时 Sentinel Dashboard 实时限流效果
- **对比数据**：同一接口"有缓存 vs 无缓存"的响应时间对比图，效果非常直观
- **GitHub 提交记录**：展示 6 周的持续开发记录，体现工程化能力

---

## 附录

### A. 完整依赖版本表

```kotlin
// gradle.properties
springBootVersion=3.2.5
springCloudVersion=2023.0.1
springCloudAlibabaVersion=2023.0.1.2
mybatisPlusVersion=3.5.7
redissonVersion=3.31.0
jjwtVersion=0.12.5
hutoolVersion=5.8.28
```

### B. IDEA 运行配置

```
1. 先启动 Docker 中的中间件
2. 启动 Nacos（如果 Docker 未启动，可本地 java -jar 启动）
3. 按依赖顺序启动服务：
   common (lib 模块，无需启动)
   → user-service → product-service → inventory-service
   → cart-service → order-service → payment-service
   → gateway（最后启动）
4. 启动前端：cd frontend && npm run dev
5. 访问：http://localhost:8080/swagger-ui.html (后端接口)
          http://localhost:3000 (管理后台)
```

### C. 快速启动脚本

```bash
# start-all.sh / start-all.bat
echo "=== 1. 启动 Docker 中间件 ==="
docker compose -f docker/docker-compose.yml up -d mysql redis rabbitmq nacos sentinel

echo "=== 2. 等待中间件就绪（30秒） ==="
ping -n 30 127.0.0.1 > nul

echo "=== 3. 构建项目 ==="
./gradlew clean build -x test

echo "=== 4. 启动后端服务 ==="
# 新开终端窗口依次启动各服务
start java -jar user-service/build/libs/*.jar
start java -jar product-service/build/libs/*.jar
# ...

echo "=== 5. 启动前端 ==="
cd frontend && npm run dev
```

### D. 轻量版减配方案（8GB 内存）

如果机器只有 8GB 内存：

```bash
# 只用核心中间件
docker compose up -d mysql redis nacos

# 去掉：
# - Elasticsearch（搜索用 MySQL LIKE 替代）
# - RabbitMQ（延迟队列用 Java ScheduledExecutorService 替代演示）
# - Sentinel（限流用 Guava RateLimiter 替代）
# - Prometheus + Grafana（监控用 Actuator 替代）
```

> **核心链路依然完整**：用户 → 商品 → 购物车 → 下单 → 支付，缓存三大问题、分布式锁、JWT 双 Token 全都在。

---

*本文档将随项目推进持续更新。每个阶段完成后，建议回到这里标记完成状态。*
